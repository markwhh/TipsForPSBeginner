<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.markwoo.tipsforpsbeginner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".Menu1Panel" />
        <activity android:name=".Menu2History" />
        <activity android:name=".Menu3Layer" />
        <activity android:name=".Menu4Shortcut" />
        <activity android:name=".Menu5Tutorial" />
        <activity android:name=".Menu6About"></activity>
    </application>

</manifest>
